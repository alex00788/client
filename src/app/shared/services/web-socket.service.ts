import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class WebSocketService {
  // socket = new WebSocket('ws://localhost:3500'); // меняем при деплое
  // socket = new WebSocket('wss://xn--80aneajyecjh1b5f.xn--p1ai_host/wss');
}
